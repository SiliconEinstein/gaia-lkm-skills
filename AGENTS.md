# gaia-lkm-skills — DEPRECATED

This repository is deprecated. Its skills have been migrated into
**[`SiliconEinstein/Gaia`](https://github.com/SiliconEinstein/Gaia)** and now ship bundled
with the `gaia-lang` package (materialized via `gaia skill register`). See `README.md`.

**Do not author or edit skills here.** All skill work — the migrated skills, their
references, and the authoring discipline — lives in Gaia now: see `gaia/_skills/` and Gaia's
own `AGENTS.md`.

## Collaboration

`main` is the source of truth. In the event a final tag is cut, every merge to `main` gets a
CalVer tag `v<YYYY.MM.DD>[.N]` referencing the PR.
